plugins {
    id 'org.scoredroid.java-module'
}

dependencies {
    api project(':core:features:common:data')
    api project(':core:features:common:domain')
    api project(':core:features:common:infra:commonmodels')
    implementation project(':core:features:common:infra:dependencies')
}
